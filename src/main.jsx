import React from "react";
import ReactDOM from "react-dom/client";
import BusinessMirror from "../BusinessMirror.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BusinessMirror />
  </React.StrictMode>
);
